import { Extension } from "./interfaces";

// Core extension
export const coreExtension: Extension = {
  name: "seij.extension-platform",

  // eslint-disable-next-line @typescript-eslint/no-empty-function
  activate: () => {},
};
