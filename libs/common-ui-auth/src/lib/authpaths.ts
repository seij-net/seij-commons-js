/**
 * Default authentication paths as expected by the views
 * when they need to say "ok but don't block this page we need it"
 */
export const AuthenticationPaths = {
  login: "/authentication-login",
  logout: "/authentication-logout",
  callback: "/authentication-callback",
};
